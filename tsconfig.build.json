{
  // Declaration-emit config for the publishable package, used by tsup (`npm run build`).
  // It narrows the program to src/lib so the bundled .d.ts only describes the library
  // (not the root sanity app or tests).
  "extends": "./tsconfig.json",
  "include": ["src/lib"]
}
